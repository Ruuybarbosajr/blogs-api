const login = require('./login.controller');
const user = require('./user.controller');
const category = require('./category.controller');
const blogPost = require('./blogPost.controller');

module.exports = {
  login,
  user,
  category,
  blogPost,
};